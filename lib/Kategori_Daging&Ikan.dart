import 'package:flutter/material.dart';

class DagingIkan extends StatelessWidget {
  const DagingIkan({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(body: Text("HALO4"),),
    );
  }
}