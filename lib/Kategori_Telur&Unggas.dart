import 'package:flutter/material.dart';

class TelurUnggas extends StatelessWidget {
  const TelurUnggas({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Text("HALO3"),
      ),
    );
  }
}
